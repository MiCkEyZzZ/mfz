generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Views {
  id    Int    @id @default(autoincrement())
  count Int    @default(0)
  slug  String
}
